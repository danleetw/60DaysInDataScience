1. 在速度較慢的時候，可以先從哪邊開始檢查？
A: 
    (1)用內建函數可能會比較快，可以嘗試用list方式、DataFrameName方式查找資料、Apply等方式測試看看
    (2)可以調整讀取方式，用PKL檔處理速度最快,HDS次之、CSV、然後XLSX最慢

2. 資料過⼤時應採取什麼⽅式讓記憶體占⽤量下降
A: 
    (1)可以降低資料精度 64bits -> 32bits
    (2)可以刪去不使用資料欄位
    (3)如果資料沒有正負值型態，可以改成無正負數值，或是有正負數，但是資料範圍差異不大，可以嘗試把整個值域移成正數再改成無正負型態